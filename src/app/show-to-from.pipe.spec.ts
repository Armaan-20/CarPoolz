import { ShowToFromPipe } from './show-to-from.pipe';

describe('ShowToFromPipe', () => {
  it('create an instance', () => {
    const pipe = new ShowToFromPipe();
    expect(pipe).toBeTruthy();
  });
});
